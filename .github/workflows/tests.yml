name: Tests

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    env:
      KANBUD_PROJECT_ROOT: ${{ github.workspace }}/libs
      PYTHONUTF8: "1"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt pytest

      - name: Compile core modules
        run: |
          python -m py_compile kanbud_bootstrap.py
          python -m py_compile campaign_data_paths.py
          python -m py_compile cn_materialy_scraper.py
          python -m py_compile cn_sync_replies_and_reminders.py
          python -m py_compile cn_province_keywords.py
          python -m py_compile cn_materialy_inquiry_email_zh.py
          python -m py_compile cn_claude_prompts.py
          python -m py_compile contact_extract_utils.py

      - name: Scraper smoke PL materialy (--test)
        run: python cn_materialy_scraper.py --test

      - name: Regresja i testy PL (pytest + unittest)
        run: |
          python -m unittest tests.test_cn_materialy_regression -v
          python -m pytest \
            tests/test_cn_inquiry_email_zh.py \
            tests/test_cn_materialy_integration.py \
            tests/test_cn_reminder_email.py \
            tests/test_repo_isolation.py \
            -q
